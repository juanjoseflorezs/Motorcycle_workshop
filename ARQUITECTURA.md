# 🏗️ Arquitectura del Proyecto - Motorcycle Workshop

## Diagrama de Capas

```
┌─────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                       │
│                  (REST Controller)                          │
│                  Prog3Controller                            │
│  (60+ Endpoints para Vehículos, Clientes, Servicios, etc)  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                    BUSINESS LOGIC LAYER                     │
│                      (Services)                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Vehicle      │  │ Client       │  │ Service      │     │
│  │ Service      │  │ Service      │  │ Service      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ SparePart    │  │ Inventory    │  │ CSV          │     │
│  │ Service      │  │ Service      │  │ Service      │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                      MODEL LAYER                            │
│                   (Domain Objects)                          │
│  ┌──────────────────────────────────────────────────────┐  │
│  │              Vehicle (Abstracta)                     │  │
│  │  ┌────────────┐  ┌────────────┐  ┌────────────┐    │  │
│  │  │ Motorcycle │  │ Car        │  │ Truck      │    │  │
│  │  └────────────┘  └────────────┘  └────────────┘    │  │
│  └──────────────────────────────────────────────────────┘  │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Client       │  │ Service      │  │ SparePart    │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │ Inventory    │  │ Maintenance  │  │ Saveable     │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│                   PERSISTENCE LAYER                         │
│                   (Data Storage)                            │
│              data/vehicles.csv (CSV Files)                  │
└─────────────────────────────────────────────────────────────┘
```

---

## Diagrama de Clases - Herencia

```
                    ┌─────────────────┐
                    │   Vehicle       │
                    │  (Abstracta)    │
                    ├─────────────────┤
                    │ - id: String    │
                    │ - brand: String │
                    │ - model: String │
                    │ - year: Integer │
                    │ - color: String │
                    │ - price: Double │
                    ├─────────────────┤
                    │ + getVehicleType()      │
                    │ + calculateMaintenance()│
                    │ + getFullDescription() │
                    └─────────────────┘
                           ▲
                ┌──────────┼──────────┐
                │          │          │
        ┌───────┴────┐  ┌──┴───────┐ ┌┴──────────┐
        │ Motorcycle │  │   Car    │ │  Truck    │
        ├────────────┤  ├──────────┤ ├───────────┤
        │ - engineCC │  │ - doors  │ │ - capacity│
        │ - transmis │  │ - fuel   │ │ - axles   │
        │ - windshld │  │ - consump│ │ - cargo   │
        └────────────┘  └──────────┘ └───────────┘
```

---

## Diagrama de Interfaces

```
┌──────────────────────────────┐
│      Saveable (Interfaz)     │
├──────────────────────────────┤
│ + toCSV(): String            │
│ + getCSVHeader(): String     │
└──────────────────────────────┘
           ▲
           │ implements
           │
    ┌──────┴──────┐
    │ VehicleCSV  │
    ├─────────────┤
    │ - vehicle   │
    ├─────────────┤
    │ + toCSV()   │
    │ + getCSVHeader()
    └─────────────┘
```

---

## Flujo de Datos - Agregar Vehículo

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Cliente HTTP                                             │
│    POST /prog3/motorcycle                                   │
│    {id, brand, model, year, color, price, engineCC, ...}  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Prog3Controller                                          │
│    @PostMapping("/motorcycle")                              │
│    addMotorcycle(@RequestBody Motorcycle motorcycle)        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. VehicleService                                           │
│    addVehicle(Vehicle vehicle)                              │
│    - Agrega a lista en memoria                              │
│    - Llama a CSVService para guardar                        │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. CSVService                                               │
│    saveToCSV(Saveable object, String fileName)              │
│    - Crea VehicleCSV wrapper                                │
│    - Serializa a formato CSV                                │
│    - Escribe en archivo                                     │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. Archivo CSV                                              │
│    data/vehicles.csv                                        │
│    ID,Brand,Model,Year,Color,Price,Type,...                │
│    MOTO001,Yamaha,YZF-R1,2023,Rojo,15000000,...           │
└─────────────────────────────────────────────────────────────┘
```

---

## Flujo de Datos - Consultar Vehículos

```
┌─────────────────────────────────────────────────────────────┐
│ 1. Cliente HTTP                                             │
│    GET /prog3/vehicles                                      │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 2. Prog3Controller                                          │
│    @GetMapping("/vehicles")                                 │
│    getAllVehicles()                                         │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 3. VehicleService                                           │
│    getAllVehicles()                                         │
│    - Retorna lista de vehículos en memoria                  │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 4. Prog3Controller                                          │
│    ResponseEntity<List<Vehicle>>                            │
│    - Serializa a JSON                                       │
│    - Retorna respuesta HTTP 200                             │
└─────────────────────────────────────────────────────────────┘
                            ↓
┌─────────────────────────────────────────────────────────────┐
│ 5. Cliente HTTP                                             │
│    [                                                        │
│      {id: "MOTO001", brand: "Yamaha", ...},               │
│      {id: "CAR001", brand: "Toyota", ...}                 │
│    ]                                                        │
└─────────────────────────────────────────────────────────────┘
```

---

## Estructura de Directorios

```
Motorcycle_workshop/
│
├── src/
│   ├── main/
│   │   ├── java/
│   │   │   └── co/edu/umanizales/motorcycle_workshop/
│   │   │       ├── model/
│   │   │       │   ├── Vehicle.java
│   │   │       │   ├── Motorcycle.java
│   │   │       │   ├── Car.java
│   │   │       │   ├── Truck.java
│   │   │       │   ├── Maintenance.java
│   │   │       │   ├── Saveable.java
│   │   │       │   ├── VehicleCSV.java
│   │   │       │   ├── VehicleExample.java
│   │   │       │   ├── Client.java
│   │   │       │   ├── Service.java
│   │   │       │   ├── SparePart.java
│   │   │       │   └── Inventory.java
│   │   │       ├── service/
│   │   │       │   ├── VehicleService.java
│   │   │       │   ├── ClientService.java
│   │   │       │   ├── ServiceService.java
│   │   │       │   ├── SparePartService.java
│   │   │       │   ├── InventoryService.java
│   │   │       │   └── CSVService.java
│   │   │       └── controller/
│   │   │           └── Prog3Controller.java
│   │   └── resources/
│   │       ├── application.properties
│   │       └── proyecto.md
│   └── test/
│       └── java/
│           └── MotorcycleWorkshopApplicationTests.java
│
├── data/
│   └── vehicles.csv
│
├── pom.xml
├── README.md
├── MODELO_DOCUMENTACION.md
├── EJEMPLOS_CURL.md
├── RESUMEN_IMPLEMENTACION.md
├── NUEVAS_CLASES.md
├── RESUMEN_FINAL.md
└── ARQUITECTURA.md
```

---

## Patrón MVC Adaptado

```
┌──────────────────────────────────────────────────────────┐
│                    VIEW (HTTP Response)                  │
│                    JSON / CSV Format                     │
└──────────────────────────────────────────────────────────┘
                            ▲
                            │
┌──────────────────────────────────────────────────────────┐
│                  CONTROLLER                              │
│              Prog3Controller                             │
│  - Recibe requests HTTP                                  │
│  - Llama servicios                                       │
│  - Retorna respuestas                                    │
└──────────────────────────────────────────────────────────┘
                            ▲
                            │
┌──────────────────────────────────────────────────────────┐
│                    MODEL                                 │
│  - Vehicle, Motorcycle, Car, Truck                       │
│  - Client, Service, SparePart, Inventory                 │
│  - Maintenance, Saveable, VehicleCSV                     │
└──────────────────────────────────────────────────────────┘
                            ▲
                            │
┌──────────────────────────────────────────────────────────┐
│                   SERVICES                               │
│  - VehicleService, ClientService, ServiceService        │
│  - SparePartService, InventoryService, CSVService       │
│  - Lógica de negocio                                     │
└──────────────────────────────────────────────────────────┘
                            ▲
                            │
┌──────────────────────────────────────────────────────────┐
│                  DATA STORAGE                            │
│              data/vehicles.csv                           │
│              (En memoria durante ejecución)              │
└──────────────────────────────────────────────────────────┘
```

---

## Dependencias entre Servicios

```
Prog3Controller
    │
    ├─→ VehicleService
    │       │
    │       └─→ CSVService
    │
    ├─→ ClientService
    │
    ├─→ ServiceService
    │
    ├─→ SparePartService
    │
    └─→ InventoryService
```

---

## Flujo de Polimorfismo

```
Vehicle vehicle = new Motorcycle(...);  // Referencia a Vehicle
                                        // Instancia de Motorcycle

vehicle.getVehicleType();               // Retorna "Motorcycle"
vehicle.calculateMaintenanceCost();     // Calcula: 50,000 × (EngineCC / 100)

---

Vehicle vehicle = new Car(...);         // Referencia a Vehicle
                                        // Instancia de Car

vehicle.getVehicleType();               // Retorna "Car"
vehicle.calculateMaintenanceCost();     // Calcula: 100,000 + (FuelConsumption × 10)

---

Vehicle vehicle = new Truck(...);       // Referencia a Vehicle
                                        // Instancia de Truck

vehicle.getVehicleType();               // Retorna "Truck"
vehicle.calculateMaintenanceCost();     // Calcula: 200,000 + (LoadCapacity × 5,000) + (Axles × 15,000)
```

---

## Flujo de Interfaz Saveable

```
Vehicle motorcycle = new Motorcycle(...);

VehicleCSV vehicleCSV = new VehicleCSV(motorcycle);  // Implementa Saveable

vehicleCSV.getCSVHeader();  // "ID,Brand,Model,Year,Color,Price,Type,EngineCC,..."
vehicleCSV.toCSV();         // "MOTO001,Yamaha,YZF-R1,2023,Rojo,15000000,Motorcycle,998,..."

csvService.saveToCSV(vehicleCSV, "vehicles");  // Guarda en CSV
```

---

## Ciclo de Vida de una Solicitud

```
1. Cliente envía HTTP Request
   ↓
2. Spring Boot recibe en Prog3Controller
   ↓
3. Controller valida y llama Service
   ↓
4. Service procesa lógica de negocio
   ↓
5. Service interactúa con Model (clases de dominio)
   ↓
6. Si es necesario, Service llama CSVService
   ↓
7. CSVService serializa datos a CSV
   ↓
8. Datos se guardan en archivo
   ↓
9. Service retorna resultado a Controller
   ↓
10. Controller serializa a JSON
    ↓
11. Spring Boot envía HTTP Response
    ↓
12. Cliente recibe respuesta
```

---

## Tecnologías por Capa

```
┌─────────────────────────────────────────┐
│ Presentation Layer                      │
│ - Spring Web (REST)                     │
│ - Jackson (JSON Serialization)          │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Business Logic Layer                    │
│ - Spring Service (@Service)             │
│ - Lógica de negocio personalizada       │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Model Layer                             │
│ - Lombok (@Data, @NoArgsConstructor)    │
│ - POO (Herencia, Polimorfismo)          │
│ - Interfaces (Saveable)                 │
└─────────────────────────────────────────┘

┌─────────────────────────────────────────┐
│ Persistence Layer                       │
│ - Java File I/O                         │
│ - CSV Format                            │
│ - En memoria (List<>)                   │
└─────────────────────────────────────────┘
```

---

## Resumen de Arquitectura

✅ **Arquitectura en Capas** - Separación clara de responsabilidades  
✅ **MVC Pattern** - Model-View-Controller adaptado  
✅ **Service Layer** - Lógica de negocio centralizada  
✅ **REST API** - Interfaz HTTP estándar  
✅ **POO** - Herencia, polimorfismo, interfaces, abstracción  
✅ **Persistencia** - Datos en CSV  
✅ **Escalable** - Fácil agregar nuevas funcionalidades  

---

*Arquitectura del Proyecto: Motorcycle Workshop - Taller de Motos*
